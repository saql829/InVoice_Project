services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: voice-backend
    ports:
      - "8001:8001"
    env_file:
      - .env
    volumes:
      - ./backend:/app
      - ./models:/models
